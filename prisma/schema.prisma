generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Produto {
  id          Int      @id @default(autoincrement())
  nome        String
  quantidade  Int
  preco       Float
  categoria   String
}